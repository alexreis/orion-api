<header class="bar bar-nav">
  {{#link-to 'orion.index' class="icon icon-left-nav pull-left"}}{{/link-to}}
  {{#link-to 'questionnaires.new' class="icon icon-compose pull-right"}}{{/link-to}}
  <h1 class="title">Questionários</h1>
</header>

<div class="content">
  <div class="content-padded">
    <form>
    {{input type="search" value=queryName tabindex="1"}}
    </form>

    <div class="card">
      <ul class="table-view">
        {{#each questionnaire in filteredQuestionnaires}}
        <li class="table-view-cell">
          {{#link-to 'questionnaires.questionnaire' questionnaire class="navigate-right"}}
            {{questionnaire.name}}
          {{/link-to}}
        </li>
        {{/each}}
      </ul>
    </div>
  </div>
</div>
